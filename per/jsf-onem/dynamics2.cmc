// Assumes basic.cmc and nodeXX.cmc are loaded.
// Adjust maxacc, maxvel and kv before calling
// the script.

define twodynamics()
{
	epos = 0;
	clock = 0;

etrack(3000,60000,0,11640,23280,34920);

	while(working)
	{
		printf("%d,%d,%d,%d\n", clock, etpos-epos, evel-194,mvel);
		pause(500);
	
	}

	pause(5000);
	etpos = 0;

//	while (working)
//	{
//		printf("%05d\n", epos);
//		pause(33);
//	}

	printf("Done\n");

}
