fwhm is a program which looks at all the brighter stars in an image,
computes median horizontal and vertical values for FWHM, and adds this
info to the FITS header as the following fields:

	FWHMH	real	horizontal median FWHM, pixels
	FWHMV	real	vertical median FWHM, pixels

fwhm: usage: [options] [file.fts ...]
  -a:      print in arc seconds if can, else pixels
  -v:      verbose
  -o:      allow overwriting any existing FWHM fields
  -w:      write header (default is read-only)
  -d:      delete any FWHM header fields (requires -w)
  -f file: name of file containing filenames.

! For RCS Only -- Do Not Edit
! @(#) $RCSfile: README,v $ $Date: 2003/04/15 20:48:34 $ $Revision: 1.1.1.1 $ $Name:  $
