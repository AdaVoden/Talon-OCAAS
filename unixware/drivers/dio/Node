# Node file for the dio driver.
# This is new in SVR4.2.
# for a complete description, read the `node' man page.
# `idinstall -k -a dio' copies this to /etc/conf/node.d/dio. (-k keeps)
# format: module-name node-name type minor user group permissions
#   node-name:	name of special file, relative to /dev.
#   type:	b or c
#   minor:	minor device number -- we use it as the ordinal entry in System
#   user:	decimal uid (optional)
#   group:	decimal gid (optional)
#   permissions:octal perms, as per chmod (optional)

dio telescope/dio96 c 0 0 0 666
dio telescope/dio24 c 1 0 0 666
