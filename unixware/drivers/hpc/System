# System file for the hpc driver.
# This is new in SVR4.2; ergo, the first non-comment line must be `$version 1'.
# for a complete description, read the `system' man page.
# `idinstall -k -a hpc' copies this to /etc/conf/sdevice.d/hpc. (-k keeps)

$version 1

# format: module-name configure unit ipl itype ivec sioa eioa scma ecma dmachan
#   configure:	Y or N
#   unit:	anything
#   ipl:	SPL level
#   itype:	interrupt type: 0:no interrupt; 1:has one, nonshareable intrpt
#   ivec:	interrupt vector
#   sioa:	starting I/O address
#   eioa:	ending I/O address
#   scma:	starting memory address, 0 if none.
#   ecma:	ending memory address, 0 if none.
#   dmachan:	dma channel, or -1 if no dma.

hpc	Y	0	0	0	0	120	12b	0	0	-1
